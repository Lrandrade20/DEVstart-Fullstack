//Iteração
let matriz = [1, 2, 3]
let soma = nota[0] + nota[1] + nota[2]
// Se tiver mais valores, precisaria fazer mais somas. E isso é um problema
// Se tivesse 50 itens, seriam 50 vezes somando

// A iteração nos permite executar uma tarefa repetitiva para cada item de uma matriz, sem a necessidade de escrever o mesmo código várias vezes.
